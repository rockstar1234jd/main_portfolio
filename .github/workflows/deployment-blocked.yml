name: Deployment Blocked

on:
  push:
  pull_request:

permissions: {}

jobs:
  fail-deployment:
    runs-on: ubuntu-latest
    steps:
      - name: Report deployment status
        run: |
          echo "Deployment is currently blocked: repository is not ready for deployment yet."
